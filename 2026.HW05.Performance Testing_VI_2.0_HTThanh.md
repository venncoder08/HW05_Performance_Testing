# HW05 – Kiểm thử Hiệu năng (Performance Testing)

## 1. Thông tin chung

| **Mã bài tập** | **HW05-AI** |
| --- | --- |
| **Thời lượng** | 10 giờ |
| **Hạn nộp** | Vui lòng xem link nộp bài trên Moodle |
| **Hình thức** | Bài tập cá nhân |
| **Nộp bài** | Moodle (báo cáo) |
| **Giảng viên & Trợ giảng** | TS. Lâm Quang Vũ / TS. Trần Duy Hoàng / ThS. Trần Thị Bích Hạnh / ThS. Trương Phước Lộc / ThS. Hồ Tuấn Thanh |
| **Liên hệ** | lqvu@fit.hcmus.edu.vn / tdhoang@fit.hcmus.edu.vn / ttbhanh@fit.hcmus.edu.vn / tploc@fit.hcmus.edu.vn / hthanh@fit.hcmus.edu.vn |
| **Chính sách AI** | Mở — bắt buộc phải khai báo và nộp kèm Báo cáo Kiểm toán AI (AI Audit Report) |
| **Cấp độ Bloom-AI yêu cầu** | G9.1 → G9.6, tùy theo từng bài tập (xem *CLO Mapping*) |

## 2. Nguyên tắc thực hiện

Những nguyên tắc này quy định cách bạn được kỳ vọng làm việc trong suốt chuỗi bài tập của môn học này. Hãy đọc kỹ trước khi bắt đầu, vì bài nộp của bạn sẽ được đánh giá dựa trên các nguyên tắc này.

- **Chiến lược AI-First.** Bạn được yêu cầu áp dụng AI vào các kỹ thuật kiểm thử đã học trên lớp. Tuy nhiên, điều này không có nghĩa là chỉ đưa ra một prompt chung duy nhất kiểu *"chạy load test và cho tôi biết hiệu năng có tốt không."* Thay vào đó, bạn phải hướng dẫn AI thực hiện từng bước của kỹ thuật đúng như đã được dạy, sử dụng AI như một trợ lý có kỷ luật chứ không phải một "hộp đen".
- **Đánh giá của con người (Human review).** Mọi kết quả do AI tạo ra phải được bạn — sinh viên — xem xét kỹ lưỡng. Bạn hoàn toàn chịu trách nhiệm về tính đúng đắn của các kết quả này. Bạn được kỳ vọng sẽ thực hiện các sửa chữa và tinh chỉnh cần thiết — việc nộp nguyên kết quả AI mà không xem xét là không được phép.
- **Báo cáo Kiểm toán AI (AI Audit Report).** Toàn bộ quá trình sử dụng AI phải được ghi lại đầy đủ trong một log. Bạn được khuyến khích xây dựng các Agent Skill có thể tự động thực hiện các hoạt động này cho các bài tập tương tự. Nếu bạn **không** sử dụng AI, bạn vẫn phải khai báo rõ điều này.
- **Tài liệu hóa.** Toàn bộ quá trình làm việc phải được tài liệu hóa dưới dạng văn bản, ví dụ như Markdown.
- **Chất lượng hơn là hoàn thành.** Bài làm của bạn sẽ không chỉ được chấm dựa trên việc có hoàn thành hay không, mà còn dựa trên số lượng và chất lượng của các sản phẩm bàn giao: test plan, file dữ liệu, log thô và các báo cáo dạng report view, minh chứng về tài nguyên/phần cứng, video demo, bài phê bình phân tích của AI, và các đường link tham khảo.

## 3. Kết quả học tập

Sau khi hoàn thành bài tập này, bạn sẽ có thể:

- Thiết kế và chạy các bài kiểm thử Load, Stress, và Spike đối với backend API của SUT bằng JMeter (hoặc k6).
- Thu thập và trình bày các chỉ số hiệu năng cùng với việc giám sát tài nguyên và nhiều dạng report view, tự xác định ngưỡng chịu tải bền vững (endurance threshold) trên phần cứng của mình.
- Sử dụng AI để phân tích kết quả, sau đó phản biện có tính phê phán đối với phân tích đó — chỉ ra những chỗ AI hiểu sai chỉ số và những đề xuất tối ưu nào của AI là khả thi.
- Đề xuất một pipeline kiểm thử hiệu năng liên tục (continuous performance-testing pipeline).
- Thể hiện năng lực Bloom-AI ở các cấp độ **G9.2 (Apply)**, **G9.3 (Analyse)**, **G9.4 (Collaborate)**, và **G9.6 (Disrupt)**.

## 4. Hệ thống được kiểm thử (SUT)

**SUT:** EShop — một ứng dụng thương mại điện tử demo bằng tiếng Việt được thiết kế để thực hành kiểm thử.

**Repository:** https://github.com/ttbhanh/eshop-sut

Các tính năng của ứng dụng được tổ chức thành các nhóm (pool) như sau:

- **Pool A — Xác thực, Danh mục và Sản phẩm**
    - FR-01: Đăng ký tài khoản
    - FR-02: Đăng nhập và khóa tài khoản
    - FR-03: Quên mật khẩu và đặt lại mật khẩu (hai bước)
    - FR-04: Quản lý hồ sơ cá nhân
    - FR-05: Danh sách và tìm kiếm sản phẩm
    - FR-06: Xem chi tiết sản phẩm
- **Pool B — Giỏ hàng và Thanh toán**
    - FR-07: Giỏ hàng
    - FR-08: Thanh toán (Checkout)
    - FR-09: Mã giảm giá (coupon)
    - FR-10: Máy trạng thái đơn hàng (Order state machine)
    - FR-11: Xem lịch sử đơn hàng (người dùng)
- **Pool C — Quản trị Web (Web Admin)**
    - FR-12: Kiểm soát truy cập
    - FR-13: Dashboard
    - FR-14: Quản lý danh mục (CRUD)
    - FR-15: Quản lý sản phẩm (CRUD)
    - FR-16: Nhập sản phẩm từ file CSV
    - FR-17: Quản lý mã giảm giá (CRUD)
    - FR-18: Quản lý đơn hàng (admin)
    - FR-19: Quản lý người dùng (admin)
- **Pool D — Ứng dụng Mobile**

SUT cung cấp một REST backend API mà web frontend sử dụng; hãy tham khảo repository để biết chính xác các endpoint và port.

## 5. Phạm vi — Lựa chọn Endpoint

Chọn ba **nhóm endpoint** backend API mục tiêu, gán mỗi nhóm vào API của SUT:

- **Read-heavy (đọc nhiều)** — ví dụ: liệt kê/tìm kiếm sản phẩm và xem chi tiết sản phẩm.
- **Auth-heavy (xác thực nhiều)** — ví dụ: đăng nhập, có xét đến hành vi khóa tài khoản.
- **Transactional (giao dịch)** — ví dụ: thêm vào giỏ hàng và thanh toán/tạo đơn hàng.

Như trong các bài tập trước, hãy đảm bảo lựa chọn của bạn **không trùng lặp** với các thành viên khác trong nhóm: không có hai thành viên nào được kiểm thử cùng một luồng nghiệp vụ (workflow).

## 6. Yêu cầu

Với mỗi nhiệm vụ dưới đây, hãy ghi lại quá trình thực hiện trong báo cáo chính và đính kèm các minh chứng cần thiết. Hãy xem lại các bài giảng liên quan về kiểm thử hiệu năng trước khi bắt đầu.

### Nhiệm vụ 1 — Thiết kế và thực thi test có hỗ trợ của AI

Theo chiến lược AI-first, hãy sử dụng một công cụ AI để thiết kế và tạo ra các test plan, sau đó xem xét, sửa chữa và chịu trách nhiệm hoàn toàn về chúng.

- **Thiết kế và tạo bằng AI.** Hướng dẫn một công cụ AI — theo từng bước, không phải bằng một prompt chung duy nhất — để thiết kế và tạo ra ba test plan: **Load**, **Stress**, và **Spike**. Cả ba test plan phải thực thi cùng một luồng nghiệp vụ end-to-end, bao quát cả ba nhóm endpoint: **auth-heavy**, **read-heavy**, và **transactional**. Ví dụ, một virtual user có thể đăng nhập, xem/tìm kiếm sản phẩm, sau đó thêm một mặt hàng vào giỏ hàng và hoàn tất thanh toán. Nhờ AI giúp chọn các tham số thực tế (think-time, ramp-up, số lượng thread/virtual-user) cho từng kịch bản, và giải thích ngắn gọn cách luồng nghiệp vụ bao quát từng nhóm endpoint.
- **Biến luồng nghiệp vụ thành data-driven.** Sử dụng dữ liệu đầu vào CSV trong luồng nghiệp vụ end-to-end để tham số hóa các request (ví dụ: thông tin đăng nhập, ID sản phẩm, hoặc payload đơn hàng). Bạn có thể dùng một hoặc nhiều file CSV, tùy theo luồng nghiệp vụ của mình.
- **Sử dụng ba dạng report view khác nhau.** Trong ba test plan, hãy sử dụng ba loại listener/report khác nhau (ví dụ: View Results Tree, Summary Report, Aggregate Report); không lặp lại loại nào. *(Thuật ngữ của JMeter; người dùng k6 cung cấp các output tương đương khác nhau.)*
- **Đặt tên cho mỗi test plan** theo mẫu `{MãSốSinhViên}_{LoạiKịchBản}_{YYYYMMDD}`.
- **Xem xét và sửa chữa (human review).** Xem xét có tính phê phán các test plan do AI tạo ra và sửa chữa chúng. Báo cáo những gì AI đã làm sai hoặc bỏ sót — ví dụ: ramp-up hoặc think-time không thực tế, số lượng thread sai, assertion yếu, hoặc thiếu xử lý khóa tài khoản — và giải thích *tại sao* AI đã bỏ sót những điều đó (do chất lượng prompt, hạn chế của model, hay đặc điểm của endpoint). Bạn hoàn toàn chịu trách nhiệm về các test plan cuối cùng.
- **Chạy đầy đủ nhất có thể, kèm minh chứng.** Thực thi cả ba kịch bản và ghi lại, với mỗi lần chạy, một ảnh chụp màn hình của công cụ cùng với mức sử dụng tài nguyên của tiến trình backend (htop/Task Manager/Activity Monitor), cùng một báo cáo phần cứng (ảnh chụp dxdiag/screenfetch và một bảng thông số kỹ thuật). Khi các lần chạy Stress/Spike kích hoạt việc khóa tài khoản sau 3 lần đăng nhập sai, hãy đặt lại (reset) trước khi chạy lần khác và ghi lại các bước đã thực hiện. Nộp các log `.jtl` thô và các thư mục HTML report.
- **Xác định ngưỡng chịu tải bền vững (endurance threshold).** Chạy một bài kiểm thử endurance/soak ngắn (khoảng 10–15 phút ở mức tải liên tục) để xác định bằng thực nghiệm ngưỡng của phần cứng bạn đang dùng, báo cáo bằng các số liệu cụ thể (ví dụ: RPS tối đa ổn định, giới hạn bộ nhớ).
- **Ghi lại một video demo.** Một video YouTube ở dạng unlisted, tổng thời lượng **ít nhất 6 phút** (bạn có thể chia thành nhiều clip, mỗi kịch bản một clip), cho thấy công cụ và trình giám sát tài nguyên **trong cùng một khung hình**, kèm lời tường thuật bằng tiếng Việt của chính bạn.
- **Báo cáo các vấn đề (issue).** Ghi lại bất kỳ lỗi hoặc vấn đề hiệu năng thực sự nào (response lỗi, crash, hồi quy chức năng) lên trang GitHub Issues của bạn kèm ảnh chụp màn hình. Việc ghi nhận các vấn đề hiệu năng như độ trễ cao hay tỷ lệ lỗi tăng cao được khuyến khích nhưng không bị trừ điểm nếu không có.

### Nhiệm vụ 2 — Phân tích bằng AI và truy tìm những chỗ AI hiểu sai

Theo chiến lược AI-first, sử dụng AI để phân tích kết quả của bạn, sau đó xem xét có tính phê phán những gì AI tạo ra — phần phân tích là sản phẩm của AI, còn phần xem xét/phê bình là của bạn.

- **Phân tích bằng AI.** Sau khi thu thập kết quả thô, hãy yêu cầu (prompt) một công cụ AI phân tích các log `.jtl` và đề xuất các ngưỡng hiệu năng (thresholds).
- **Xem xét và sửa chữa (human review).** Xem xét có tính phê phán phân tích của AI và chỉ ra những chỗ AI hiểu sai hoặc đọc sai các chỉ số. Với mỗi lỗi hiểu sai, hãy trích dẫn **giá trị đúng từ log `.jtl` thô** của bạn và giải thích lỗi đó.
- **Đánh giá các đề xuất của AI.** Yêu cầu AI đề xuất các phương án tối ưu (ví dụ: thêm chỉ mục database, connection pool, hoặc bật SQLite WAL) và phân loại từng đề xuất là **khả thi hoặc ảo tưởng (hallucinated)**, kèm lý giải.

### Nhiệm vụ 3 — Đề xuất mô hình Kiểm thử Hiệu năng Liên tục (Disrupt)

- Trong phần kết luận, hãy đề xuất một **mô hình kiểm thử hiệu năng liên tục** theo dõi các commit của SUT, quyết định có nên chạy kiểm thử hiệu năng hay không, và cảnh báo các hồi quy p95. Bao gồm một **flow chart (sơ đồ luồng)** và phần thảo luận về các **đánh đổi (trade-off)** (chi phí, cảnh báo giả).

## 7. Agent Skill

- Bạn được khuyến khích xây dựng một Agent Skill áp dụng quy trình kiểm thử hiệu năng và phân tích log này, để có thể tái sử dụng cho các endpoint khác trong các bài kiểm thử tương lai.
- Nộp kèm skill này cùng với một video demo (link YouTube) cho thấy toàn bộ quá trình bạn sử dụng skill đó trên một nhóm endpoint hoàn chỉnh.

## 8. Công cụ được phép và Cấp độ Bloom-AI

Bạn có thể sử dụng các công cụ sau, và phải khai báo chúng trong Báo cáo Kiểm toán AI:

- JMeter (mặc định) hoặc k6 (điểm cộng).
- Bất kỳ công cụ AI nào bạn chọn (ví dụ: ChatGPT, Claude, Gemini) — để phân tích log.
- Một công cụ giám sát tài nguyên (htop/Task Manager/Activity Monitor).

Cấp độ Bloom-AI yêu cầu cho bài tập này là **G9.2 (Apply)**, **G9.3 (Analyse)**, **G9.4 (Collaborate)**, và **G9.6 (Disrupt)**.

## 9. Báo cáo Kiểm toán AI (Phụ lục bắt buộc)

Đính kèm Báo cáo Kiểm toán AI như một phụ lục. Sử dụng nội dung của các AI Template đã cho nếu cần.

- Nếu bạn không sử dụng AI, hãy khai báo: *"Tôi không sử dụng bất kỳ sự trợ giúp của AI nào trong bài tập này."*
- Nếu bạn có sử dụng AI, hãy khai báo: *"Tôi sử dụng công cụ AI cho các nhiệm vụ sau,"* và cung cấp các thông tin sau cho mỗi lần tương tác:
    - Tên công cụ AI
    - Ngày và giờ
    - Prompt của bạn
    - Kết quả đầu ra của AI

Để đơn giản hóa quá trình này, bạn được khuyến khích tạo một skill hoặc rule tự động trích xuất các thông tin trên sau mỗi phiên làm việc với AI.

## 10. Phê bình AI (AI Critique) (200–300 từ, bắt buộc)

Viết một đoạn văn 200–300 từ phê bình AI. Trả lời các câu hỏi sau: AI đã làm sai, thiên vị (bias), hoặc thiếu sót ở đâu? Tại sao nó không nhận ra được vấn đề đó? Bạn đã học được nguyên tắc gì về việc hợp tác với AI qua bài tập này?

Sử dụng nội dung của các AI Template đã cho nếu cần.

## 11. Các ràng buộc chống gian lận AI (Anti-AI-Cheat Constraints)

Bài tập này dựa trên các minh chứng thực thi thực tế, có thể quy trách nhiệm. Những mục sau **không được** tạo bởi AI hoặc bị làm giả, và trợ giảng sẽ kiểm tra chúng khi chấm bài:

- Tên file test plan, phải khớp với mẫu `{MãSốSinhViên}_{LoạiKịchBản}_{YYYYMMDD}`.
- Các file log `.jtl` thô, đính kèm đầy đủ — không chỉ phần tóm tắt.
- Video demo, phải cho thấy công cụ và trình giám sát tài nguyên trong cùng một khung hình, kèm lời tường thuật bằng giọng của chính bạn.
- Báo cáo phần cứng, có hostname khớp với các lần triển khai ở các bài tập trước của bạn.

## 12. Nhật ký Commit Git

- Tạo một commit Git mới cho mỗi bước trong quy trình (ví dụ: test plan của mỗi kịch bản, phần phân tích AI, và đề xuất kiểm thử liên tục).
- Cung cấp nhật ký commit Git dưới dạng file văn bản.

## 13. Bảo vệ vấn đáp (Oral Defense)

**30% sinh viên** được chọn ngẫu nhiên có thể được mời tham gia bảo vệ vấn đáp trong 5–7 phút vào tuần sau hạn nộp bài, để giải thích cách bạn đã hoàn thành bài tập này.

## 14. Quy định nộp bài

- **Định dạng tên file:** `<MãSốSinhViên>_HW05_AI_Performance_<ĐiểmTựĐánhGiá>.zip`
    - *ĐiểmTựĐánhGiá:* một số gồm 3 chữ số, trong khoảng [000, 100].
    - *Ví dụ:* `25127001_HW05_AI_Performance_090.zip`
- **Nội dung bắt buộc của file `.zip`:**
    - Báo cáo chính (Markdown + PDF), bao gồm báo cáo kiểm thử hiệu năng và phần phê bình phân tích AI của bạn.
    - Link repository GitHub công khai (test plan và file dữ liệu).
    - Ba test plan (Load/Stress/Spike) theo đúng quy ước đặt tên file.
    - Ba log `.jtl` thô và ba thư mục HTML report.
    - Ảnh chụp màn hình trình giám sát tài nguyên và thông số phần cứng.
    - Link video demo YouTube dạng unlisted.
    - AI Critique và AI Audit Report (Markdown + PDF).
    - Nhật ký commit Git (file văn bản).
    - Báo cáo lỗi (bug report), kèm ảnh chụp màn hình các issue trên trang GitHub Issues (nếu có).
    - Một file `README.md` chứa bảng tự đánh giá (bên dưới) và một báo cáo tóm tắt kiểm thử: các kịch bản đã chạy; các nhóm endpoint đã bao quát; ngưỡng chịu tải bền vững (kèm số liệu); số lượng bug/vấn đề hiệu năng; và link video demo.
    - Bất kỳ tài liệu hỗ trợ khác.
- Nộp lên Moodle. Về hạn nộp, hãy xem link nộp bài.

## 15. Mẫu đánh giá

| **STT** | **Tiêu chí** | **Điểm** | **Tự đánh giá** |
| --- | --- | --- | --- |
| **1** | Nhiệm vụ 1 — Load testing | 20 |  |
| **2** | Nhiệm vụ 1 — Stress testing | 20 |  |
| **3** | Nhiệm vụ 1 — Spike testing | 20 |  |
| **4** | Nhiệm vụ 2 — Phân tích AI + truy tìm chỗ hiểu sai (kèm giá trị đúng từ log thô) | 10 |  |
| **5** | Nhiệm vụ 3 — Đề xuất Kiểm thử Hiệu năng Liên tục (G9.6) | 10 |  |
| **6** | Agent Skills | 10 |  |
|  | **Tổng** | **100** |  |

## 16. Tài liệu tham khảo

- ISTQB Foundation Level Syllabus (phiên bản mới nhất).
- Hardman, P. (2025). *A Post-AI Learning Taxonomy.*
- Fuster Rabella, M. (2025). *OECD Education Working Paper No. 338.*
- Anthropic (2025). *Building Reliable AI Test Agents* — engineering blog.
- Tài liệu DeepEval & Promptfoo — các framework kiểm thử LLM.

## 17. Các quy định khác

- **Không được phép** nộp bài muộn.
- Thiếu bất kỳ tài liệu bắt buộc nào sẽ bị **0 điểm**.
- Sao chép giữa các sinh viên — **bao gồm cả prompt** — sẽ dẫn đến **cả hai bên bị 0 điểm**.
